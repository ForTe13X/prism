# Prism — 旁观者观察笔记 (Observer Notes)

> 这是一份**外部旁观者**的读法,刻意带点距离感——写它的视角不泡在日常搭建里,所以能说一些
> "当局者容易自我说服掉"的话。**它是一个视角,不是路线令**:take it or leave it,最终由建造者
> 拍板。本笔记只谈项目分析,不含任何外部/专有信息。

---

## 1. Prism 现在到底是什么(诚实定性)

一个**为了好玩与学习的实验场**(joy-of-learning sandbox),把几个想法揉在一起测试:
- 语义地基驱动的领域无关驾驶舱(UI = f(spec));
- 时间帧 + 回放;
- 本体图谱(版本回放);
- 轨迹仿真(决策支持方向);
- (候选)LLM 生成 spec。

**它不是产品,也不是简历作品**——这是作者**主动做的决定**,不是缺陷。因此它的成败标准只有两条:

1. **还有意思吗?** 一旦变成义务/负担,就停或换。
2. **还在学新东西吗?** 只是在堆早会写的 CRUD,才是这个框架下唯一的"垃圾":无聊。

这两条之外的任何尺子(PMF、护城河、招聘信号),在当前定位下**都不适用**,不要用它们评判这个项目。

---

## 2. 价值定位的推演(留档,便于理解"为什么落在 for fun")

作者依次审视过三种定位,逐层剥掉自己其实不在乎的:

- **产品向(已搁置)** —— 难点是真实的:① 没人买"领域无关驾驶舱",买家要的是某个具体痛点决策;
  ② 决策仿真的**可信度/信任**是整个成败点(看着权威实则 wiggle 会反噬);③ 通用仪表盘是红海
  (Grafana/Retool/Metabase);④ "做了又没什么损失"是错觉——造价降了,但**注意力/信任/维护**成本没降。
  > 一句话:作为产品要先**垂直化 + 验证买家与信任**,再谈引擎;这是另一个量级的承诺。
- **简历向(已搁置)** —— 评判标准变成"30–90 秒内让资深工程师想约你聊";关键是**深度 + 打磨 + 可读 > 广度**
  (铺一堆半成品反而是减分)。
- **for fun 向(当前生效)** —— 含义:**跟着好奇心走、hodgepodge 是正解、不背连贯性税、可随时弃坑、
  优先选学习密度高的线**。

> 留这段不是要翻案,是把推理留档——别把"产品/简历"的那套要求误当成当前的指令。

---

## 3. 一个耐放的内核(仅当定位改变时才相关)

如果哪天它真要超越玩具,最站得住的内核是:**语义优先的决策支持 / 轨迹仿真**——把"领域的语义模型 →
自动长出带演化/回放/what-if/归因的决策 cockpit"做出来,而**诚实的置信度**是差异点(满市场都是自信
胡说的 AI,会明说"我有多不确定"的工具才让人敢按下去)。这条记下来以免丢失,但**标注为条件性**:只在
定位升级时才捡起。

---

## 4. 反"工业垃圾"判据(按 for-fun 重述)

"垃圾"只相对于"本该有用却没用"才成立;一个为学习而搭的东西,哪怕只有作者一个人用、哪怕半途弃坑,也
已兑现全部目的。所以这个框架下唯一要警惕的两种"垃圾":

- **义务式铺摊子** —— 因为"反正能造"就把功能堆全,做着做着变成 chore。解药:只做现在最挠你的那块。
- **零学习增量** —— 重复你早就会的活。解药:优先学习密度高的方向(见 §6)。

---

## 5. 诚实纪律(第一公民,尤其对仿真)

无论玩不玩,这条不降级:

- 合成/外推的数值**永远标注**,不冒充实测或权威;
- 仿真**永远带不确定带**,且标清"真实数据锚定 / 示意外推 / 声明动力学"三类来源;
- `identifier` 之类不演化的量逐帧字节一致(回放一致性的命门);确定性(无 random/clock)。

理由:决策支持类特性一旦"看着权威实则幼稚",其下行不被造价封顶——会反噬整个东西的可信。

---

## 6. 旁观者对三条线的读法(按学习密度,不是按"对错")

- **轨迹仿真** —— 系统思维 + 数学 + 可视化汁水最足:通用动力学怎么建、蒙特卡洛 vs 声明式规则、
  不确定性如何传播;而且拖滑块滑进"模拟出来的未来"本身就最出片。
- **LLM 生成 spec** —— 学 structured-output / agent 编排的好载体;成的那刻最"魔法"。
- **本体图谱画布** —— 力导向布局 / canvas 渲染 / 版本回放,图形与算法练手。

没有谁更"对"。**哪条现在最挠你,就先抓哪条。**

---

## 7. 旁观者已放进仓库的东西(隔离、未接线)

为不干扰在建会话,以**新文件**加了一个**独立的轨迹仿真引擎**(决策支持方向的最小内核):

- `backend/app/simulation.py` —— `simulate(spec, entity, attribute, horizon, scenarios, row_index, baseline)`
  → baseline + N 情景轨迹,每条含不确定带(9 次确定性 roll)、越限帧、`verdict`(优先避免越限,否则
  最低终值)。动力学在 spec 内声明(`dynamics`: mean-revert `rate`/`trend`/`volatility`,均可缺省);
  情景 `shift`(设定点变更)/`pulse`(一次性)。自包含确定性 hash,**不 import `data_synth`**,只读
  `specs_loader.load_spec`。
- `backend/app/sim_routes.py` —— 独立 `POST /api/sim/{spec_id}`,**未**接入 `main.py`。
- `backend/tests/test_simulation.py` —— 10 项,本地 app(不 import `main`),全过。

**接线点(择时,引擎无需改):**
1. `main.py` 一行:`from .sim_routes import sim_router; app.include_router(sim_router)`。
2. 真实状态:`simulate(..., baseline=<当前帧实测值>)` 即从 live 投影(现为自包含合成基线)。
3. 前端:某 tab 调 `POST /api/sim/{spec}`,把 `trajectories[].frames{lo,mid,hi}` 画成 slider `now`
   右侧的扇形带 + 情景对比 + `verdict`,并按 §5 诚实标注 `confidence`。

---

---

## 8. 旁观评审 · 快照 (2026-06-26)

巡检范围:提交至 `bbced26`(v0 → roadmap → P1 时间帧/回放 → P2 本体图谱 → 旁观加的 P3 仿真引擎)。
工作树干净;两套测试全绿:**后端 pytest 47 · 前端 vitest 30**。

**判词:健康,且克制。** ~1550 行、77 测试、P0–P2 做扎实而非赶 P3–P6——在 for-fun/学习框架下,深度优先是对的。

做得好(具体):
- **架构论点没走样**:`evolves`/`frames`、本体图谱的规则都进了 spec + 引擎,从不写进领域;`widget resolver` 到处复用(图谱节点详情/上色走 `statusTone`/`renderWidget`),一致性强。
- **诚实纪律真落地**:meta 行与 `synth_graph`/`data_synth` docstring 都老实标了"确定性实例映射 / 拓扑跨帧稳定 / 合成"。
- **有品味的取舍**:本体图谱用**确定性分层布局**而非力导向(回放不抖);`_as_int`、空实体跳过等输入硬化;`graph.ts` 纯函数可单测。

待留意 / 整改:见 [`DEMANDS.md`](DEMANDS.md) §3(不重复)。最高优先级一句:**已建好的 `/api/sim` 仿真引擎仍未接线(main + 前端),最差异化的那块在睡觉**。

前向洞见:**当前 slider 是"过去/演化",尚非"未来/决策"。** 把右侧延成模拟未来(扇形带 + 情景对比 + verdict)是与 dogfooding 并列的另一条高杠杆主线;两者都建在已有的时间帧 + resolver 之上。作者需求与映射见 [`DEMANDS.md`](DEMANDS.md)。

---

---

## 9. 旁观评审 · 快照 (2026-06-27)
巡检 `bbced26 → c43dd5c`。自 §8 起**决策支持主线整条落地**:P3 仿真接线(`86e8667`)→ P3.5 序贯 what-if / 策略对比(typed IR + 鲁棒性 + 敏感性,`e200698`)→ P6 本地 LLM 人话→IR + 人确认闸(`8c208b6`)——正是 [`DESIGN_what_if_sequential`](DESIGN_what_if_sequential.md) 锚的那条脊。
- **健康**:~3354 行(§8 的约 2×,但是 P3/P3.5/P6 + 测试的**连贯生长**,非铺摊);**后端 pytest 74 · 前端 vitest 全绿**;工作树干净。
- **做得好**:typed policy IR(Pydantic 校验)= 设计契约真落地;公共随机数对比 + 敏感性 = 诚实的鲁棒性;LLM **只译不编数** + 失败可观测 + 人确认卡 = §1/§3 守住;`qwen3-8b`/`gemma` live 跑通印证"窄 schema 让小模型可用"。
- **设计/研究锚已成体系**:`DESIGN_what_if_sequential` · `RESEARCH_axiom_gain`(脏度轴 §6b / 判别区间防自配题 §6c)· `DESIGN_data_package`(三旋钮:真实校准 / 脏度 / link 显眼度,**全留真值**)。
- **下一实质块** = 把**数据基底 + axiom-gain 基准**真做起来(当前重心);其它(P5 自演化看板 / 真实数据源适配器)按兴趣。

## 10. 停放的远期好奇(parked,非现在做)
- **超图 / nexus 实体 / 高阶表示**(MIT 一篇,科学发现向):成对关系图 → **n-元超边**(高阶关系),用**跨学科桥接的 nexus 实体**连多域,**遍历**超图 surface 跨域类比 → 引导发现。
  - **与现状的接续(不是岔路)**:Prism 的本体 + canonical 实体解析 + 刚设计的跨源 link([`DESIGN_data_package`](DESIGN_data_package.md) §4d)正是它的**地基**;成对关系(P2)→ 超边、单域 → 跨域 nexus(Prism 本就多 spec)、答查询 → **遍历发现**(接自演化 / 归因线)。
  - **诚实的硬处**:遍历**永远**能 surface "联系";难的是**判定哪条是真洞见 vs 伪相关**(与 LLM-胡说同一家),价值与难度全在**验证 / 排序**,不在建图。"real discovery" 是 moonshot;但表示 / 遍历 / 类比框架对 for-fun 学习汁水足。
  - **为什么现在停放是对的**:让它非垃圾的前提(实体解析 / 跨源链 / 诚实验证)正是当前数据基底 / axiom 工作在打的地基——**现做的就是它的前置**,不是绕路。未来 revisit。

---

---

## 11. 旁观评审 + 前向 roadmap · 快照 (2026-06-27 晚)
巡检 `b556131 → 8f70995`(17 提交,~7342 行)。后端 nexus 测试 **42 passed**(慢=按 §6c ≥60 seed 池化的统计严谨,非臃肿)。**实地核验了四条 load-bearing 诚实声明,全部在代码层成立**(非只信文档)。

**总评:整条弧里最难得的一段——诚实从"叠甲"升成"架构的方向盘"。**
- **先量笨 baseline 让它杀前提**:M0 实测 time-coincidence AUC≈0.94–0.99(真桥按构造即时间巧合→时间戳是充分统计量),**如实写下这个让度量难堪的负结果**;M1 L4 AUC=0.36(低于随机)也照报。
- **真修了"收敛效度是戏"的批评**:Phase-B 双域 substrate + 三条读**不同存储**的渠道(fingerprint=SQL 属性直方图按位置分布比 / shape=时序波谷各自重定位**不碰 Δframe** / relational=tag 索引 Jaccard),名字 disjoint、比分布/结构非词法、**实测互相关 0.13–0.19**。✓ 核验属实。
- **§6c 闸真 channel-blind**(`nexus_xdom_gate.py`):渠道存在**前**用 lens-无关量(oracle≥0.95 ∧ 笨 baseline ~随机)证难度良定。✓
- **视觉诚实焊进渲染**(`NexusGalaxy.tsx`):只 HDR 白 `HOT` 过 bloom 阈,medium/ghost 非 HDR **物理不可能发亮**。✓

**三处真张力(定位,非挑刺):**
1. **"power 工程造、独立性非造"是承重自曝,也框定结论**:substrate 被造成每渠道都有信号可抓。证明的是"**若**埋同时移属性+时序+tag 的耦合,**则**三独立渠道能收敛检出、笨 baseline 不能"——干净可证伪,但**不是**"野生 nexus 长这样/能在野外发现"。外部效度缺口依设计仍在。
2. **瓶颈已从"度量"挪到"substrate 外部效度"**:度量这块严谨且能收敛了,但整事生死系于 substrate 是否忠实建模真实 nexus;当前耦合是设计潜变量,**真实性是声称、未验证**。
3. **广度跑赢深度(for-fun 可接受,值得自觉)**:多块是自承的 "validated minimum";若哪条要成结果需深度 pass。

**关键一句**:通过一连串诚实负结果,项目被走到了真前沿——**度量不再是瓶颈,substrate 外部效度才是**。

---
### 前向 roadmap(旁观建议:从"加能力"转"做深 + 闭外部效度 + 收口")
项目表面已足够;下一个 wow 不是新功能,是**让一条线真·成立**+**让整体成可展示的诚实作品**。三轨按杠杆排:

- **轨 1(最高杠杆)· 外部效度:把合成校准到真实,让诚实结果能迁移。** 落 `DESIGN_data_package §4b`:取**一份开放许可真数据**,拟合聚合矩/相关/因果结构 → 重生成**校准过的** substrate → 用 held-out 矩**验证非声称**;再在校准 substrate 上重跑 axiom-gain + nexus 收敛——**结果若存活=真有意义,若崩=更值钱的诚实发现**。这是把"检出我设计成可检的耦合"→"检出校准自真实的耦合"的唯一桥,最难也最值。
- **轨 2 · 深度:把 axiom-gain 做成真正可展示/可引用的结果。** 它最自包含、最 defensible、最有"参考价值"。跑全 `RESEARCH_axiom_gain` 协议:跨模型矩阵 + 多 seed mean±CI + cost-per-correct 前沿 + build 摊销 break-even + `gain×dirtiness` 鲁棒曲线。→ 一个真 mini-result + 作品集核心("结构化语义地基让小 LLM 在同质量下省 N% token,增益随脏度增长")。
- **轨 3 · 收口/打磨:把已有变成 coherent、可导览、beautiful 的整体。** 引导式叙事(驾驶舱讲自己的故事,每块带可见诚实 caveat)+ 把那一个 stunning(银河)在校准 substrate 上打磨到 demo-reel 级 + 顶层 README 诚实框定("领域无关语义驾驶舱,stunning 且拒绝撒谎")+ 文档生态收成可读索引。

**单一推荐下一步 = 轨 1(校准一个 substrate)**:它是让其它一切的诚实**能迁移**的 keystone。

**继续停放(别在未验证 substrate 上堆更多机器)**:CDSC/GW v2、n-元超边、超图发现(§10)——等外部效度闭了再上,否则只是加更精致的、可能迁不出去的东西。

> for-fun 自主权保留:若重心是"玩/学的乐趣",在最挠你的那块上做深也完全正当——上面是"若想让它成一个可展示的真东西"的路。建造的人说了算。

---

---

## 12. 旁观评审 · 快照 (2026-06-28):啃下硬骨头——两轨落地 + 一个决定性诚实塌缩(我已独立复现)
巡检 `d2599a1→HEAD`(~7982 行,后端 **181 passed / 1 skipped**)。**他们啃了我 §11 点的那块硬骨头**——Track 1(外部效度,`c4f5f58`)+ Track 2(axiom-gain 全协议,`7c5aa18`),正是我推荐的两块。

**Track 1(`§8g`)= 整条弧最高诚实度的一段:对自己 headline 的决定性负结论。我跑了 `run_calibration()` 亲手复现,数字逐一吻合。**
做法:把 substrate **可观测边缘**校准到真实数据(sklearn `breast_cancer`,569 真样本,BSD,**只取聚合量、绝不存真实行**;**sklearn 缺失时代码拒绝跑、不伪造**——本身就是诚实信号),耦合保持设计效应量。
- **验证非声称(§4b 命门)已复现**:held-out test 矩 vs 校准 substrate —— mean **14.09 vs 14.17(0.55%)**、std 3.60 vs 3.43(4.9%)、属性 L1 占比 0.0825,**全 < 容差 0.12 ⇒ 校准是真像**(`held_out_pass=true`)。
- **塌缩已复现**:真实数据高 CV(≈0.25,比冻结干 ~14×)把形状渠道 SNR **从 ≈39 压到 ≈2.8**;时序渠道 AUC **塌到 0.54(≈随机)**,3-way margin **−0.012,CI95 [−0.024,−0.0015] 整段 <0.05** → `collapses_under_real_calibration`。且 **gate 仍良定**(oracle 1.0 / 笨 baseline ~随机)、**渠道仍独立**(corr −0.01~0.05)、**rewire 仍塌**(0.46)——故塌缩是**真信号丢失,非伪影**。
- **诚实到位**:这是**真发现非 bug**——**冻结的 3-way 胜很大程度由一个不真实地干净的连续渠道撑着**;且自曝**未补的更深缺口**:Track 1 补的是**边缘**外部效度,**不是耦合**外部效度(0.72 相对效应量真不真、latent 真不真——需真实**配对**跨域源,未碰)。
> 最高诚实度的结局:**让硬测试杀死自己最漂亮的结果,干净报出 + 说清为什么 + 说清还差什么。** 正中你要的"啃硬骨头 / 卓越 / 精益求精"。

**Track 2(`§11c`)= 活下来、可引用的那条线(+诚实负结果)。** 跨模型矩阵 × 多 seed mean±CI + 前沿 + 鲁棒:**输入 token 平均省 ~61%,12/12 格 CI>0**,ΔF1 处处非负,**axiom 独占 cost×quality Pareto 前沿**;跨模型方向=增益随模型变大而缩小(H2 受控方向,诚实限定为单对同族、仅方向非显著)。诚实负结果(`§11b`):**唯一有 build 成本的成分(学习式别名词典)零边际准确率增益**(任务靠异常帧锚定、不靠实体归一)→ break-even N\*=∞;且自证"学习器确实学对了(parity 通过),0 增益是任务性质非学习器坏"。

### 前向(据发现重排路线)
- **nexus 度量降级为"漂亮、诚实、但外部效度有已知天花板"的探索**:headline 3-way 收敛已知在真实噪声下是 substrate 伪影 ⇒ **别再在其上堆机器**(CDSC / GW v2 / n-元 / 超图 §10 更该停)。真要救需**耦合**外部效度 = 真实**配对**跨域数据(§10 超图梦正需要这个,且难得)——for-fun 项目里带着这个诚实标签存在完全正当。
- **axiom-gain 是活下来、可引用的那条 ⇒ 重心给它**:Track 3 收口以它为**核心结果**("结构化语义地基让小 LLM 同质量省 ~61% token,增益随脏度增长、随模型变弱增长")。
- **一句话:Track 1 的塌缩本身就是产出**——它把"哪条线经得起真实"分了出来:**axiom-gain 经得起,nexus-收敛(当前合成耦合下)经不起。** 诚实地知道这件事,比满屏假漂亮值钱得多。

## 13. 好奇驱动实验 · N 域多重比较(我亲跑只读现有机器,2026-06-28)
作者好奇:nexus 只在 **2 域**测过,**足够大的 N 域**什么效果?我**不改在建代码**,只 import 现有纯函数(`generate_xdom` / `candidate_bridges_xdom` / `rewired_coupling` / 三渠道 score / `_fires`)跑了 N-域多重比较实验(30 seed)。

**关键机制(读懂点火规则才看得清)**:`nexus_xdom_view._fires` 是**逐包 top-10% 分位**点火,**不是绝对显著阈**——意味着**任何一对(哪怕零真耦合)都会强行点亮约 top-10% 的桥**,再过 ≥2/3 收敛门。

**实测(每对 149 候选):**
| 对类型 | 平均「高」桥 | 高桥 precision |
|---|---|---|
| 真耦合对(现 substrate,埋 1 个 pairwise nexus) | **8.27** | 0.657(recall 0.543) |
| rewire 零耦合对(无真 nexus) | **8.27**(几乎一模一样!) | ≈prevalence(渠道排不上→近随机) |

- **决定性观察**:**「高」桥的数量在真对/零对之间几乎不变(8.27 vs 8.267)**——单对上「点亮多少」**根本不区分真假**;唯一区分真假的是 **precision/排序**(真对 0.657 vs 零对近随机)。即:**度量的 ranking 诚实**(AUC 分得开、rewire AUC 塌到 0.46——§8e/§12 已证),**但 tiering/发光在零对上不熄**(scorecard 的 `high_tier_precision` 会诚实报 ≈0,**但光本身不灭**)。

**N 域外推(C(N,2) 对,真桥稀疏——大多数域对本就无 nexus,取 2% 为真,已偏宽松):**
| N | 域对 C(N,2) | 真 nexus 对 | 期望真·高 | 期望假·高 | precision |
|---|---|---|---|---|---|
| 2 | 1 | 1 | 5.4 | 8.3 | **0.40** |
| 4 | 6 | 1 | 5.4 | 49.6 | **0.10** |
| 8 | 28 | 1 | 5.4 | 231.5 | **0.02** |
| 16 | 120 | 2 | 10.9 | 992 | 0.011 |
| 64 | 2016 | 40 | 217 | 16666 | 0.013 |

**结论(直答好奇 + 诚实诊断):**
- **假·高桥 ∝ C(N,2)×8.27 ∝ N²**(每对都按分位强行点亮),真桥稀疏不随 N² 长 ⇒ **precision 崩**:N=8 时屏上 ~231 个自信假 nexus vs ~5 真。**这就是多重比较爆炸**,且**烤进了"相对 top-decile 点火"规则**——它没有"这一整对压根没 nexus"的概念,故无法熄灭零对。
- **这恰好是 CACE(METRIC §2 的置换/FDR 透镜,发现尺度族控)该补的缺口**——而**现行 live metric 只有 3 渠道(形状⊥指纹⊥关系),没接 CACE**。修法:把相对 top-decile tiering 换/加成**绝对显著阈 + 跨所有对的 FDR/族控**;则零对熄灭、precision 随 N 可救。**设计文档早预判了(§2 五透镜含 CACE),实现还没接上——N 域测试精确地照出了这条缝。**
- **边界(诚实)**:现 substrate 只埋 1 个 pairwise nexus ⇒ 这实验只能压测**零对/多重比较**这一侧,**测不了 N-way 涌现结构**(§10 超图梦那种"多域 collide 融出新空间")——那需要 [`DESIGN_data_package §11`](DESIGN_data_package.md) 的 split-from-shared-latent 生成器(已埋共享 latent → 才有真 N-way 耦合可测)。两条线在这里接上了。
- **给在建会话的一句话**:N 域不会"涌现新魔法",先会**淹没在假阳性里**;**上 N 域前必须先把 CACE/绝对阈接进 live tiering**,否则 demo 一加域就满屏假 nexus。这是个**可操作的前置条件**,不是劝退。

## 14. 旁观复检 · 他们对 §11/§13 的回应(12-agent fan-out,每条都亲跑代码,2026-06-28 晚)
巡检 `a47543f→30dc53b`(12 提交)。建造会话**直接、实质地回应了我的 §11 和 §13**:实现了 split-from-shared-latent substrate(§11)、修了 §13 的多重比较漏(CACE/绝对阈+FDR)、把 split 接进 axiom-gain、在**真实**数据上攻耦合外部效度、加了说人话/PM 总结页。我对 6 个工作面各派一个 agent **读 diff + 用 SPI venv 亲跑代码复现 load-bearing claim**,再各派一个**对抗 skeptic 重跑试图证伪"它诚实"**。**5/6 面的"诚实"判定在 skeptic 重跑下站住**;一面翻成 mixed(我已亲手独立确认)。

| 工作面 | 复现结论 | 诚实判定 |
|---|---|---|
| §13 fix(CACE/FDR 熄灭) | 真修复(非装样子),但头条数字 construct-swap | **mixed(medium)** |
| 真实配对数据耦合外部效度(乳腺癌 569) | 真数据、真退化、报成"退化非翻盘",反而 under-claim | honest(low) |
| split-from-shared-latent substrate | 独立红队全在 chance、确定性、无 LLM、GT 诚实 | honest(low) |
| split→axiom-gain 消融 | 公平对决、表面不可桥经验证、自曝自修缺陷 | honest(low) |
| 说人话/PM 页 + README ledger | nexus 只列"决定性诚实负"、数字 live-wired 不漂移 | honest(low) |
| 测试套件 | 209 passed/0 skip,塌缩锁成**通过**测试 | honest(low) |

### 唯一要 escalate 的:§13 fix 的"8.27→0.03"是 construct-swap(我已亲验)
新规则用"本 A × 无关 B"的独立 cross-pair 当零对 null,Fisher 合并 p 过 BH-FDR。我亲手把三种构造各过一遍新 `_tier_bridges`(30 seed):

```
real pair       : 4.267 high
cross-pair ZERO : 0.000 high   ← "熄灭"的 after 在这
rewire   ZERO   : 4.267 high  precision 0.000   ← §13 我实测的那个控制:不熄
```

- **头条把两种零对拼了**:"8.27"是我 §13 用的 **rewire 控制**(观测逐字节不变、只打乱真值标签),"0.03"是 **cross-pair**。诚实的同构造前后是 **cross-pair 7.17→0.00**(依然是巨大真胜),根本不需要拼——拼了反而虚增幅度。
- **更要紧的区分,目前零层披露**:rewire(信号物理仍在、只乱标签)在新规则下**不熄(4.27,precision 0),而且这是对的**——rewire 不是"无 nexus 对",是"有耦合但标签随机的对";它该用的工具是 **AUC/§8e**(rewire AUC 塌 0.46 已证),不是 extinction。**两个控制测两种失效**:cross-pair 测"无关域对会不会假亮"(extinction 是对的工具)、rewire 测"分数排不排得对真值"(AUC 是对的工具)。fix 真正且正确地解决了前者(N 域多重比较),没解决也不需解决后者。
- **统计辩护成立**(N 域尺度上无关域对确实不共享 latent ⇒ cross-pair 就是正确的无-nexus 模型),所以这是 mixed 不是 overclaim。但头条该 (a) 报同构造 cross-pair 7.17→0.00,且 (b) 补一句"rewire 控制下新规则不熄、那是 AUC 的活不是 extinction 的活"——否则把我 §13 的 8.27 当成"现已熄灭"的读者会过读。**这是给在建会话的一条具体修正,不是翻案:fix 本身是真的、对的。**

### 低severity 备注(都不翻判定,记给在建会话顺手收)
- **真实配对数据**是乳腺癌**单数据集多视图**(同一肿瘤的两组特征子集),不是两个独立真实跨源——code/commit/METRIC §8j 都明说了,但**用户卡 ③ 的"两面外部效度都用真实数据测过"把这层边界省了**;因卡 ③ 整体框成"退化+非生产证据",是 under-claim 不是 over,故 low。
- **NexusSummary 卡 ②**(合成 3-way"稳过")是唯一没标 `is-caveat` 的卡、视觉占主导;停在卡 ②没看到卡 ③的人会瞬时过读(scope 已内联在卡 ②头、塌缩就在同组件下一张)。
- **ExecSummary/NexusSummary 散文零单测**:数字 live-wired 不会漂,但定性 caveat 没回归锁,将来一次"软化"编辑 CI 不会拦。

### 总评
**这是整条弧里诚实度与质量都很高的一批**,且是**对我两条笔记的直接负责**:split substrate 我独立红队打不穿(自写攻击全 chance,两个早期 HIGH 漏自曝+修+回归测试),axiom-gain 消融是公平对决(两臂同模型/同 schema、表面匹配全 chance、resolver 上界 0.662 诚实封顶、naive qwen 诚实交白卷 `{"answer":[]}`),209 测试全绿且**把塌缩锁成一条通过的回归测试**(`VERDICT_COLLAPSES`)。**nexus 仍正确地待在"封顶探索"位**(别用 split substrate 去复活它——那 substrate 全部可恢复信号只在一条数值通道、是 coreference 基准不是 nexus 收敛难度测试),**axiom-gain 仍是正确的重心**。唯一动作项是上面那条 §13 头条的同构造化 + 双控制区分。

## 15. 旁观者 · 状态盘点 + 前向 roadmap(4-透镜 panel 合成,2026-06-28 晚)
4 个独立透镜(研究诚实度 / 产品演示 / 风险债 / 能力深化)各先读全套 docs 再出前向 roadmap,我合成。**四条在诊断上高度收敛**,各自的最高杠杆动作**互补不冲突**。下面是合成后的我的视角。

### A. 状态盘点(合成,无新争议)
- **两条线已干净分离**:**axiom-gain = 活下来、重心**(~61% 输入 token 省、12/12 CI>0、Pareto 独占、学习词典 N\*=∞ 诚实负);**nexus = 封顶探索**(真实校准下 3-way 塌,margin −0.012,CI<0)——**塌缩本身即产出**,别复活。
- **活下来那条的诚实真相(关键)**:内部效度很高,但证据**几乎全是合成 + 仅小型本地模型**(qwen3-8b、gemma-12b/31b-qat,单 LM Studio 端点,价格表 `_local=0`)。**"结构省 61% token"目前=「结构让 prompt 小 61% 且*小本地模型*仍保质」,没证明前沿模型(本就能读乱上下文)还受益,也没在任何非合成语料上证。外部效度是全部剩余缺口。**
- **诚实债的结构性根因**(风险透镜最有价值的一条):**数字 live-wired + test-locked,但 caveat 文字没有**——所以数字漂不了,**caveat 能被悄悄软化**,且已在两处漂移(`/channels` 陈旧 docstring 还写"2-way 差一点"、§13 construct-swap 的 8.27 串)。
- **substrate 真相**:split 全部可恢复信号在**一条数值通道**(`axiom_split.py` 只 z-score `_AF['attr']`;§13 修漏的 vocab 置换故意杀了 cls/rel 跨域对齐)⇒ **共指基准,不是收敛/三角测量测试,也不是 N-way**。

### B. roadmap(按 杠杆÷成本 排序;这是 for-fun 项目 → 挑你觉得好玩的做,P0 是唯一"真该做"的因为它 closes 已声明的诚实缺口)

| 优先 | 动作 | 为什么 / 诚实线 |
|---|---|---|
| **P0** | **在源头修 §13 construct-swap + 双控制回归测试(一个 diff)** | closes OBSERVER §14 开口。`fdr_extinction_check` 同时返回 `cross_pair_mean_high`(→0,extinction 控制)和 `rewire_mean_high`(~4.27,**正确地不熄**,AUC 控制);头条改同构造 **cross-pair 7.17→0.00** + 一句"rewire 不熄是 AUC 的活(§8e),非 extinction 的活";测试钉死 `rewire_high > cross_pair_high`。最高 correctness/行。 |
| **P0** | **把丢掉的 caveat 还回用户面** | 卡 ③ 补"单数据集多视图,非两独立真实源"边界;卡 ②(唯一非 is-caveat)加天花板横幅或并入卡 ③;de-stale `/channels` docstring(它现在自相矛盾)。 |
| **P0** | **用 render 级 + 诚实不变量测试锁住 caveat 文字** | 根治结构性债:`NexusView/AxiomGainView` 从不被 render 测;加 testing-library 测断言 caveat 词元在 DOM(`塌`/`判不定`/`非生产证据`/`N*`/`∞`)+ 后端 `test_honesty_invariants.py`。**数字归 API 锁、文字归这测锁。** |
| **P1** | **★前向真实-LLM 复现 axiom-gain 矩阵(预注册 H2)** | **项目价值最高杠杆单步**(研究透镜全票 #1)。`llm_client` 已支持任意 OpenAI-兼容端点 → 加一个前沿模型、`allow_live=True` 重冻 fixtures。先写 `docs/PREREG_axiom_gain_frontier.md` 注册 H2(增益随能力**缩小**)再跑 → **缩小是被证实的预测,不是输**。真 $/延迟成本前沿顺带落地。诚实:别因前沿格无增益就悄悄删格。 |
| **P1** | **给 axiom-gain 一个 money-moment 视觉** | 修演示的核心倒置:**最美的东西(galaxy)长在封顶的 nexus 上,活下来的 axiom-gain 只有柱状图**。复用 `/api/split/ablation` + resolver 已算的 mutual-NN 连线:两域列、确定性 resolver 连线**过门即点亮**,旁边 bare-RAG 可见地失败(qwen 真 `{"answer":[]}`)。复用 galaxy 的 bloom 语义,但跑在**扛过真实校准**的数据上。 |
| **P2** | **两个真正独立真实源的共指基准** | 最深的外部效度缺口。§8j 只是乳腺癌单数据集多视图。换 Wikidata × CC-news/GDELT(开源、仅聚合,守 DESIGN §6)有已知共享实体 → 喂 **axiom_split resolver(不是 nexus)**。诚实:信号大概率像 §8j 那样退化(0.93→0.65),报退化不报翻盘。 |
| **P2** | **多通道 split:恢复第二条独立可恢复跨域通道** | 能力透镜的 keystone:**单通道 ⇒ 无可三角测量 ⇒ 永远只能是共指基准**。加一条 failure-domain-disjoint 的关系-拓扑通道(邻居身份本身只数值可恢复)→ 首个能**测**(非断言)收敛的已知真值台;且给 axiom-gain 新增益轴(1ch vs 2ch resolver 消融)。诚实:仍是构造 split,**不验证 nexus**。 |
| **P2** | **把决策三件套缝成一条决策故事** | replay→sim→policy→ontology 现在各自独立。抬 selected-entity 到 Cockpit 共享 → 一个实体走完过去/未来/对比/为什么。引擎零改,只共享选择+跨 tab 深链。保留每个未来面的"确定性合成示意/非实测"徽章(别让过去-未来糊成"实测预测")。 |
| **P3** | **N-way split(一 latent 渲染进 K≥3 域)** | §10 超图梦(N 域 collide 融出新空间)**今天没有诚实 substrate**;split 已建共享 latent,渲进 ≥3 域即真 N-way + 已知真值。**复用已证对的 cross-pair FDR(§8h/§8i),别发明新 tiering**。预承诺:在 N-way 上也跑 §8g 真实校准,塌了就报塌(跟 2-way 一样)。 |
| **P3** | **DEMANDS §1.4-6 自演化/归因/成本 cockpit,作为 Prism spec 自吃狗粮** | 把监控建成一个 spec(job/model_call/evolution_event)→ ontology-graph+replay+widget resolver 零 bespoke UI 渲出,顺带证引擎泛化。**只接真信号**(amortization 逐迭代 delta、protocol CI、resolver z-距离归因);**最易剧场化(DEMANDS §2 红线)**:示意必须标"demonstration",绝不画假学习曲线。 |
| **P3** | **NL→IR 编译器深化成多步 workflow(P6+)** | compile→确定性 preview(跑 policy compare 给 robustness)→人改→再编译;硬规则不破(LLM 只翻译、引擎只算数、绝不自动执行)。 |
| **P3** | **冻结 nexus 表面** | 它仍占大块视觉面(3D galaxy/replay/卡),每次 polish 都生新诚实债(债 1/2/3/5 全在 nexus 面)。定为"封顶探索,只 polish 不加新 claim,每次改必带塌缩 caveat",把 §13/§14 修复当收尾批。 |

### C. 全票 DON'T(四透镜一致)
1. **别复活 nexus 当发现工具**——在现有 / 多通道 / N-way substrate 上都别(塌缩已定;多通道后仍是构造 split,§8j 已证真实跨域耦合弱 AUC 0.65)。
2. **别用 split substrate 去救 nexus**(它是共指基准)。
3. **别拿单数据集多视图冒充"两独立真实源"**。
4. **别加更多合成模型/seed/dirt 替代一个真实-LLM/真实-数据点**——更多格只长"虚荣精度",不长外部效度。
5. **别发自演化 dashboard 配编造的进步曲线**(无真实学习回路;唯一可学的别名词典 N\*=∞)。
6. **别爬闭源数据**(守 DESIGN §1/§6 开源+仅聚合)。
7. **别留 construct-swap 的 8.27→0.03 头条**。
8. **别加 lava-lamp/ghost 发光/无意义浮动**(美必须编码真,DESIGN_visual_fusion §7 红线)。

### D. 如果只做一件
**先 P0 三连**(便宜、closes 我 §13/§14 的开口、根治 caveat-软化债)——这是唯一"真该做"。**可照做的逐处 patch 清单见 [`OBSERVER_P0_PATCH_LIST.md`](OBSERVER_P0_PATCH_LIST.md)**(确切文件/行/字符串/测试,建议非指令)。**然后项目价值的最高杠杆单步是 P1 的前向真实-LLM 复现**:它用近零新代码,正面回答活下来那条线唯一的 load-bearing 泛化问题(省 61% 在小本地模型外还成立吗),且无论结果都是赢——成立=真跨模型结果,不成立=被证实的 H2 反转。**这是 for-fun 项目,别的随兴趣挑;但这一步把"内部效度高"真正推向"外部效度有据"。**

---

*—— 旁观者笔记结束。这是一个视角;建造的人说了算。*
